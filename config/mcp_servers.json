{
  "mcpServers": {
    "example-filesystem": {
      "description": "File system operations via MCP",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/workspace"],
      "transport": "stdio",
      "enabled": true
    }
  }
}
